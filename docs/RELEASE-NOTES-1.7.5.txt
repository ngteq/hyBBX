HyBBX v1.7.5 release notes

Command registry loaded from share/commands.yaml at startup.
/help, /menu, /index, /alias and verb access are registry-driven.
One blank line before /command output.

Transports: telnet :2323, SSH :3232, WebSocket loopback :4591, HBX hub :7323.
Main + Secondary topology; optional mains_proxy mesh (partial).
Built-in [security] bans; optional SQLite storage.

Proxy relay and proxymail delivery not available yet.

See docs/RELEASE-1.7.5.md, docs/TOPOLOGY.md, docs/FEATURES.md.
